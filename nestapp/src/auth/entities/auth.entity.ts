import { ObjectType } from '@nestjs/graphql';
// import { Location } from 'src/locations/entities/location.entity';

@ObjectType()
export class Auth {
  // @Field()
  // accessToken: string;
  // @Field()
  // locations: [Location];
}
